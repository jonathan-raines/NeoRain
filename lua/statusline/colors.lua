local M = {}

M.vi_mode_colors = {
  n = 'Green',
  i = 'Blue',
  v = 'Magenta',
  V = 'Magenta',
  -- OP = colors.green,
  -- BLOCK = colors.blue,
  R = 'Red',
  Rv = 'Red',
  -- ENTER = colors.cyan,
  -- MORE = colors.cyan,
  -- SELECT = colors.orange,
  -- COMMAND = colors.magenta,
  -- SHELL = colors.green,
  -- TERM = colors.blue,
  -- NONE = colors.yellow
}

return M
